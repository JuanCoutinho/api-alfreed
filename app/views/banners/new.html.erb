
<br><br>
<div class="max-w-2xl mx-auto bg-gray-800 bg-opacity-90 hover:bg-opacity-90 p-8 rounded-lg shadow-lg transition duration-300">
<h1 class="text-3xl font-bold text-white mb-6 text-center">Criar Novo Banner</h1>
  <%= form_with(model: @banner, local: true, class: 'space-y-6') do |form| %>
    <% if @banner.errors.any? %>
      <div class="bg-red-500 bg-opacity-30 border border-red-600 text-red-300 p-4 rounded-lg">
        <p class="font-semibold mb-2">Erro(s):</p>
        <ul class="list-disc pl-5">
          <% @banner.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
        </ul>
      </div>
    <% end %>

    <!-- Campo de imagem -->
    <div class="flex flex-col">
      <%= form.label :image, 'Imagem do Banner', class: 'text-gray-200 font-medium mb-1' %>
      <%= form.file_field :image, class: 'border border-gray-600 rounded-lg px-4 py-2 bg-gray-900 text-gray-200 focus:outline-none focus:ring focus:ring-indigo-500' %>
    </div>

    <!-- Campo de link -->
    <div class="flex flex-col">
      <%= form.label :link, 'Link do Banner', class: 'text-gray-200 font-medium mb-1' %>
      <%= form.text_field :link, class: 'border border-gray-600 rounded-lg px-4 py-2 bg-gray-900 text-gray-200 focus:outline-none focus:ring focus:ring-indigo-500' %>
    </div>

    <!-- Botão de envio -->
    <div>
      <%= form.submit 'Criar Banner', class: 'w-full bg-indigo-500 bg-opacity-80 text-white font-semibold py-3 rounded-lg hover:bg-indigo-600 transition duration-300' %>
    </div>
  <% end %>
</div>
