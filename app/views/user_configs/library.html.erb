<br><br>
<main class="px-4 sm:px-6 md:px-12 bg-opacity-50 text-white rounded-lg">
  <div class="py-8 bg-black bg-opacity-50 rounded-lg">

    <h1 class="text-4xl font-bold text-center text-white mb-6">Biblioteca de Bots</h1>
    <p class="text-center text-gray-400 mb-8">Baixe os Melhores Bots para o seu negócio</p>

    <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 p-3 gap-6">
      <% @user_configs.each do |config| %>
        <div class="flex flex-col items-center bg-gray-900 bg-opacity-50 rounded-lg p-6 shadow-lg transition-transform transform hover:scale-105 hover:bg-opacity-70">
          <!-- Imagem do Bot -->
      <div class="w-20 h-20 bg-cover bg-center rounded-full mb-4" 
       style="background-image: url('<%= config.imagem.attached? ? url_for(config.imagem) : asset_path("default_image.jpg") %>');">
       </div>

          <!-- Nome e Descrição -->
          <h3 class="text-lg font-semibold text-white mb-2"><%= config.nome_bot %></h3>
          <p class="text-sm text-gray-400 mb-4"><%= config.finalidade || "Sem descrição" %></p>

          <!-- Ações -->
         <div class="flex flex-col sm:flex-row space-y-2 sm:space-y-0 sm:space-x-4">
     <%= button_to "Salvar", save_user_config_path(config), method: :post, 
      class: "text-sm font-medium px-4 py-2 rounded-lg bg-gradient-to-r from-blue-500 to-purple-600 text-white shadow-md hover:from-blue-600 hover:to-purple-700 hover:shadow-lg transition-transform transform hover:scale-105 focus:ring-4 focus:ring-blue-300 focus:outline-none cursor-pointer" %>

        </div>
        </div>
      <% end %>
    </div>
  </div>
</main>
